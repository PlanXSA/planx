# Plan X — عقد الحسابات (طبقة قواعد المخزن)

الحساب ليس شاشة قياس. الحساب صف في المخزن يُكتب مع الهندسة في نفس `apply`.
العرض يقرأ الرقم المخزّن. لا يحسب طولاً ولا مساحة من بلاط 3857.

تاريخ التأسيس: 2026-09-16. نواة. ليس محرك كود بعد.

---

## 1. المبدأ

```
رسم → طوبولوجيا → كتابة هندسة → اشتقاق متري → التزام ذري
فشل أي حلقة = لا صف جديد
```

- المخزن والإحداثيات: `EPSG:4326`.
- البلاط: `EPSG:3857` للعرض فقط.
- الأمتار: مسقط UTM لمنتصف حدود المشروع (`projects.utm_epsg`)، لا Web Mercator.
- المحرك الحالي يسقط الرؤوس إلى UTM (`packages/geo/crs.ts`) ثم يحسب الطول والمساحة على المستوي. `measure_epsg` رقم المنطقة (32638 للرياض) وليس 0.

## 2. ما يُحسب في v0 وما لا يُحسب

| الكمية | على أي `kind` | عمود مخزن | متى تُملأ | ملاحظة |
|---|---|---|---|---|
| `length_m` | street, path, utility | `features.length_m` | عند `apply` لخط | مجموع القطع |
| `area_m2` | parcel, green | `features.area_m2` | عند `apply` لمضلع مغلق | قيمة مطلقة |
| `frontage_m` | parcel | `features.frontage_m` | عند `apply` | أطول ضلع مؤقتاً، ويُعلَّم تقديراً |
| `edge_count` | parcel | داخل `props` | عند `apply` | عدد أضلاع الحلقة الخارجية |
| `width_m` | street | داخل `props` فقط | يدوي أو من وسم | ليس اشتقاقاً هندسياً في v0 |
| جيران / اتجاه | parcel | داخل `props` لاحقاً | بعد محرك الجوار | خارج هذه الخطوة |
| حجم / كتلة | — | — | — | خارج v0 |
| نسبة تغطية مبنية | parcel | `props.coverage` لاحقاً | يحتاج مبنى أو فرضية استعمال | ليس الآن |

## 3. قواعد القبول قبل الرقم

تُرفض الضغطة قبل أي كتابة رقم:

| الشرط | السبب | `RejectReason` |
|---|---|---|
| مضلع قطعة غير مغلق أو أقل من 4 رؤوس في الحلقة | لا مساحة | `unclosed` |
| تقاطع ذاتي ظاهر | مساحة كاذبة | `self_intersection` |
| مساحة مشتقة تحت حد أدنى للمشروع (افتراضي 1 م² حتى يُعرَّف كود) | ضوضاء رسم | `below_min_area` |
| خط شارع أقل من نقطتين | لا طول | يُرفض في المحرك قبل `apply` |

التداخل مع قطعة أخرى: تعارض ظاهر، لا حذف صامت (`overlap_parcel` أو صف في `conflicts`).

## 4. أعمدة المخزن (قواعد لا شاشات)

تُضاف على `features` كقيم مشتقة مخزّنة — ليست مولَّدة داخل SQL في النواة لأن الاشتقاق يتم في TypeScript عند `apply` ثم يُكتب:

- `length_m DOUBLE` — NULL إن لم يكن النوع خطاً.
- `area_m2 DOUBLE` — NULL إن لم يكن النوع مضلعاً مساحياً.
- `frontage_m DOUBLE` — NULL إلا للقطعة.
- `measure_epsg INTEGER` — رقم مسقط الأمتار المستخدم لملء الصف (UTM أو 0 = تقريب محلي موثَّق).
- `measure_at TIMESTAMP` — لحظة الاشتقاق. إن تغيّرت `geom` دون تحديث هذه الحقول فالصف غير صالح للوحة.

قيود:

- `length_m IS NULL OR length_m >= 0`
- `area_m2 IS NULL OR area_m2 >= 0`
- `frontage_m IS NULL OR frontage_m >= 0`
- الشارع/المسار: `area_m2` تبقى NULL.
- القطعة/الأخضر: `length_m` تبقى NULL (المحيط إن لزم لاحقاً عمود `perimeter_m` منفصل، لا نعيد استخدام الطول).

`props` يبقى JSON للتفاصيل (رؤوس، أضلاع، `frontage_estimated`). الأعمدة أعلاه هي ما تُفلتر عليه اللوحة والجدول.

## 5. هوية المشروع مربوطة بالحساب لا بالحساب السحابي

على `projects` تُحفظ هوية محلية لأن الرقم بلا سياق مشروع رقم يتيم:

- `planner_name` اسم المخطط
- `org_name` الجهة
- `city_name` المدينة
- `title` اسم المشروع

لا بريد ولا كلمة سر في النواة. الملف المحفوظ يحمل الهوية مع الأرقام.

`utm_epsg` يُشتق من خط طول مركز الحدود عند إنشاء المشروع ويُقفل. تغيير الحدود بعد وجود صفوف مقاسة يعيد وسم `measure_epsg` على الصفوف المتأثرة في `apply` لاحق، لا صمتاً.

## 6. مصدر الحقيقة للوحة

```
SELECT kind, use, length_m, area_m2, frontage_m
FROM features
WHERE project_id = ? AND surface = 'project'
```

ممنوع:

- جمع أطوال من إحداثيات الشاشة.
- `ST_Length` على 4326 واعتبار الناتج أمتاراً.
- `ST_Area` على 3857.

مسموح لاحقاً: دوال Spatial على هندسة محوَّلة إلى `utm_epsg` داخل المحرك، على أن تُكتب النتيجة في الأعمدة نفسها.

## 7. ترتيب البناء بعد هذا العقد

1. أعمدة القياس + هوية المشروع في `schema.sql` (هذه الخطوة).
2. المحركات الحالية تملأ الأعمدة لا `props` فقط.
3. الواجهة تعرض الرقم المخزّن بجانب الرسم.
4. ثم كود الحد الأدنى للمساحة حسب `use` — قاموس مفتوح يكتبه أحمد، ليس قائمة مغلقة.
